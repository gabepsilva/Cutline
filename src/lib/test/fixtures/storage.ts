import type { StorageUsage } from '$lib/types/storage';

/** Fixture: sidebar storage widget from design lines 47–50. */
export const fixtureStorageUsage: StorageUsage = {
	percentUsed: 62,
	usageLabel: '38.4 GB of 60 GB used'
};
