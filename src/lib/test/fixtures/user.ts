import type { User } from '$lib/types/user';

/** Fixture: sidebar user from design lines 52–55. */
export const fixtureUser: User = {
	id: 'user-alex',
	name: 'Alex Chen',
	initials: 'AC',
	planLabel: 'Pro plan'
};
