import type { Clip } from '$lib/types/timeline';

/** Fixture: V1/CC clip from the design timeline prototype. */
export const fixtureTimelineClip: Clip = {
	leftPct: 2.5,
	widthPct: 18,
	label: 'Welcome to Cutline'
};
