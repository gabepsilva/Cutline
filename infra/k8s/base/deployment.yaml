apiVersion: apps/v1
kind: Deployment
metadata:
  name: cutline
  namespace: cutline
  labels:
    app.kubernetes.io/name: cutline
spec:
  replicas: 1
  minReadySeconds: 5
  progressDeadlineSeconds: 300
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: cutline
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cutline
    spec:
      automountServiceAccountToken: false
      # GHCR package is private — pull secret synced by 1Password operator (see README).
      imagePullSecrets:
        - name: cutline-regcred
      terminationGracePeriodSeconds: 30
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: cutline
          # Overlay/CD sets the real tag (kustomize images / `kubectl set image`).
          image: ghcr.io/gabepsilva/cutline:bootstrap
          imagePullPolicy: IfNotPresent
          # Source /app/.env before entrypoint until the running image ships the
          # dotenv loader in docker-entrypoint.sh (rebuild + deploy after merge).
          command: ['/bin/sh', '-ec']
          args:
            - |
              if [ -f /app/.env ]; then set -a; . /app/.env; set +a; fi
              exec /app/docker-entrypoint.sh
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - configMapRef:
                name: cutline-config
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 384Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: app-dotenv
              mountPath: /app/.env
              subPath: dotenv
              readOnly: true
            # Writable scratch for the ephemeral SQLite DB + bun cache (HOME=/tmp).
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: app-dotenv
          secret:
            secretName: cutline-app
        - name: tmp
          emptyDir: {}
