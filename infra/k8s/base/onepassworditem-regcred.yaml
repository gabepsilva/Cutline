# Syncs 1Password item "GHCR pull jpegs3 kubernetes" to Secret `cutline-regcred`
# (kubernetes.io/dockerconfigjson) for ghcr.io pulls. Shared homelab GHCR PAT — same
# item as jpegs3/image-works when the token has read:packages for gabepsilva images.
# Duplicate into a Cutline-specific 1Password item later if you want per-app isolation.
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: cutline-regcred
  annotations:
    operator.1password.io/auto-restart: "true"
spec:
  itemPath: vaults/abcm7u74m6iahz5l5cytfl3tka/items/q7jtlzkvyovs2ylv7jfp4j5myu
type: kubernetes.io/dockerconfigjson
