# Syncs 1Password item "Cutline-PROD" (homelab7) to Secret `cutline-app` (key `dotenv`).
# The item must have a text field labeled `dotenv` (not notesPlain alone) — the operator
# maps labeled fields to Secret keys. Mounted at /app/.env; Bun loads it at startup.
# Refresh `spec.itemPath` if the item is recreated in 1Password.
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: cutline-app
  annotations:
    operator.1password.io/auto-restart: 'true'
spec:
  itemPath: vaults/abcm7u74m6iahz5l5cytfl3tka/items/nzqatdqmjtjc4vgmszqznafsnm
