# One-time registration on tk8s:
#   kubectl apply -f infra/k8s/argocd/cutline-prod-app.yaml
#
# Public repo — no repo-creds required. Argo tracks the CI-managed `deploy/prod`
# branch (not `master`, which stays review-gated): deploy.yml force-pushes
# `master` + the pinned images.newTag to `deploy/prod`, and Argo syncs (T-13).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cutline-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/gabepsilva/Cutline
    targetRevision: deploy/prod
    path: infra/k8s/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: cutline
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
